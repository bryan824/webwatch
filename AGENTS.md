# Agent Instructions

- After making any repository change, commit all intended changes with a clear commit message.
- Push the commit to `origin` on the current branch.
- If committing or pushing fails, report the exact error and stop for user guidance.
- Before committing, review `git status` and `git diff` to avoid including unintended files.
